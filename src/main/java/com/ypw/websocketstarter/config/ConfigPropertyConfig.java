package com.ypw.websocketstarter.config;

import com.ypw.websocketstarter.properties.ConfigProperties;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * @author hongmeng
 */
/*
@Configuration
public class ConfigPropertyConfig {
    @Value("${websocket.time.sessionIdMaxAlive}")
    private Long sessionIdMaxAlive;
    @Value("${websocket.time.stopPullMsgInterval}")
    private Long stopPullMsgInterval;

    @Bean
    public ConfigProperties configProperty() {
        ConfigProperties configProperty = new ConfigProperties();
        configProperty.setSessionIdMaxAlive(sessionIdMaxAlive);
        configProperty.setStopPullMsgInterval(stopPullMsgInterval);
        return configProperty;
    }
}*/
